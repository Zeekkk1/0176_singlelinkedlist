#include <iostream>
using namespace std;

class node
{
public :
   int nomhs;
   node *next;
};

class LinkedList
{
    Node *START;


    public:
    LinkedList(){
        START = NULL;
    }

    void addnode()
    {
        int nim;
        cout << "n\Masukan Nomor Mahasiswa : ";
        cin >> nim;
        Node *nodeBaru = new Node;
        nodeBaru->noMhs = nimm

    }
    if (START == NULL || nim <= START->noMhs)
    {
        if ((START != NULL) && (nim == START->noMhs))
        {
            cout << "\"
        }
    }
}